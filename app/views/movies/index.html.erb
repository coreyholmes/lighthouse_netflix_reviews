<div class="index-wrapper">
<!-- ****************** Action ****************** -->


<div class="index-category-first"><h3>[ Action ]</h3></div>
<hr class="divider-top">    

<div class="movie-slider">
  <% @movies.each do |movie| %>
    <div>
      <div class="fade">
        <a data-toggle="modal" data-target="#movie<%= movie.id %>"><%= image_tag(movie.image.to_s) %></a>
        <span class="index-movie-title"><%= truncate(movie.title, length: 20) %></span><br>
        <%= year(movie.release_date) %>
      </div>
    </div>
  <% end %>
</div>

<!-- <hr class="divider-bottom">  -->

<% @movies.each do |movie| %>
  <div class="modal fade" id="movie<%= movie.id %>" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-body">
          <div class="row">
            <div class="col-sm-6">
            <%= image_tag(movie.image.to_s) %>
            </div>
            <div class="col-sm-6">
              <span class="white">
              <h3><%= movie.title %></h3>
              <p><strong>Directed by:</strong> <%= movie.director %><br>
              <strong>Length:</strong> <%= movie.runtime_in_minutes %> minutes<br>
              <%= movie.review_average %></p></span>
              <hr>
              <%= movie.description %>
              <br><br>
              <!-- <button type="button" class="btn btn-danger btn-sm">Edit</button>
              <button type="button" class="btn btn-danger btn-sm">Delete</button> -->
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
<% end %>



<!-- ****************** Drama ****************** -->


<div class="index-category"><h3>[ Drama ]</h3></div>
<hr class="divider-top">  

<div class="movie-slider">
  <% @movies.each do |movie| %>
    <div>
      <div class="fade">
        <a data-toggle="modal" data-target="#movie<%= movie.id %>"><%= image_tag(movie.image.to_s) %></a>
        <span class="index-movie-title"><%= truncate(movie.title, length: 20) %></span><br>
        <%= year(movie.release_date) %>
      </div>
    </div>
  <% end %>
</div>

<% @movies.each do |movie| %>
  <div class="modal fade" id="movie<%= movie.id %>" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-body">
          <div class="row">
            <div class="col-sm-6">
            <%= image_tag(movie.image.to_s) %>
            </div>
            <div class="col-sm-6">
              <span class="white">
              <h3><%= movie.title %></h3>
              <p><strong>Directed by:</strong> <%= movie.director %><br>
              <strong>Length:</strong> <%= movie.runtime_in_minutes %> minutes<br>
              <%= movie.review_average %></p></span>
              <hr>
              <%= movie.description %>
              <br><br>
              <!-- <button type="button" class="btn btn-danger btn-sm">Edit</button>
              <button type="button" class="btn btn-danger btn-sm">Delete</button> -->
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
<% end %>


<!-- ****************** Sci-Fi ****************** -->


<div class="index-category"><h3>[ Sci-Fi ]</h3></div>
<hr class="divider-top">  

<div class="movie-slider">
  <% @movies.each do |movie| %>
    <div>
      <div class="fade">
        <a data-toggle="modal" data-target="#movie<%= movie.id %>"><%= image_tag(movie.image.to_s) %></a>
        <span class="index-movie-title"><%= truncate(movie.title, length: 20) %></span><br>
        <%= year(movie.release_date) %>
      </div>
    </div>
  <% end %>
</div>

<% @movies.each do |movie| %>
  <div class="modal fade" id="movie<%= movie.id %>" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-body">
          <div class="row">
            <div class="col-sm-6">
            <%= image_tag(movie.image.to_s) %>
            </div>
            <div class="col-sm-6">
              <span class="white">
              <h3><%= movie.title %></h3>
              <p><strong>Directed by:</strong> <%= movie.director %><br>
              <strong>Length:</strong> <%= movie.runtime_in_minutes %> minutes<br>
              <%= movie.review_average %></p></span>
              <hr>
              <%= movie.description %>
              <br><br>
              <!-- <button type="button" class="btn btn-danger btn-sm">Edit</button>
              <button type="button" class="btn btn-danger btn-sm">Delete</button> -->
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
<% end %>

<%#= link_to movie.title, movie_path(movie) %>
<%#= format_date(movie.release_date) %>
<%#= movie.review_average %>
<%#= movie.director %> 
<%#= movie.runtime_in_minutes %>
<%#= movie.description %>
<%#= link_to image_tag(movie.image.to_s), movie_path(movie) %>

</div>